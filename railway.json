{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && python execution/chunker_embedder.py --persona ${PERSONA_ID:-jane-jacobs} && (python execution/chunker_embedder.py --persona ${PERSONA_ID:-jane-jacobs} --discourse || true)"
  },
  "deploy": {
    "startCommand": "python execution/api_server.py",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
